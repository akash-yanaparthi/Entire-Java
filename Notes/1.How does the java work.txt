How does the java work??

In every system, it will have a JVM


Hardware->OS->JVM stays here

Java is platform independent, but JVM is not

SO the jvm will understand only byte code not the java code


java code - compiler - bytecode(JVM executes this) - searches for main method - output


Main is the entry point of the program

The byte is created after we click on run and the extension of the byte code is .class whereas for code file it is .java

ok so now how does JVM runs?

The answer is JRE which will have JVM and inside JVM we will have libraries


JDK->JRE->JVM




SO The Java is WORA(write once and run anywhere)

